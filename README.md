# 涂色乐园（ColorFun）

面向儿童的涂色绘画微信小程序，基于 uni-app（Vue 3）开发。

## 开发

```bash
npm install
npm run dev:mp-weixin    # 开发
npm run build:mp-weixin  # 构建，产物在 dist/build/mp-weixin
```

微信开发者工具导入 `dist/build/mp-weixin` 或 `dist/dev/mp-weixin` 进行预览。
